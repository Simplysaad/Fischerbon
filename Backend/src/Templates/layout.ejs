<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= subject %> - Fischerbon</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        color: #333;
        background-color: #f4f6f8;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 600px;
        margin: 30px auto;
        padding: 20px;
        background-color: #fff;
        border: 1px solid #ddd;
        border-radius: 6px;
      }
      .header {
        text-align: center;
        padding-bottom: 20px;
        border-bottom: 1px solid #eee;
      }
      .header img {
        height: 60px;
      }
      h1 {
        color: #2a7ae2;
        font-weight: normal;
      }
      p {
        line-height: 1.5;
        font-size: 15px;
      }
      .footer {
        margin-top: 30px;
        font-size: 12px;
        color: #777;
        border-top: 1px solid #ddd;
        padding-top: 10px;
        text-align: center;
      }
      a {
        color: #2a7ae2;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <img src="<%= logoUrl %>" alt="Fischerbon Logo" />
      </div>

      <!-- Main email content -->
      <%- body %>

      <div class="footer">
        <p>This email was sent by <strong>Fischerbon</strong>.</p>
        <p>
          If you have any concerns, contact our support at
          <a href="mailto:<%= supportEmail %>"><%= supportEmail %></a>.
        </p>
        <p>
          &copy; <%= new Date().getFullYear() %> Fischerbon. All rights
          reserved.
        </p>
      </div>
    </div>
  </body>
</html>
