<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>FischerBon - CAD Platform</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            /* background-color: #f7f7f7; */
            margin: 0;
            padding: 0;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        .email-container {
            max-width: 600px;
            margin: auto;
            /* background-color: #ffffff; */
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            /* background-color: #0047ab; */
            padding: 20px;
            text-align: center;
        }
        .header img {
            width: 120px;
            height: auto;
        }
        .content {
            padding: 30px 40px;
            /* color: #333333; */
            font-size: 16px;
            line-height: 1.5;
        }
        .content h1 {
            color: #0047ab;
            font-size: 24px;
            margin-bottom: 15px;
        }
        .content p {
            margin-bottom: 20px;
        }
        .footer {
            /* background-color: #f1f1f1; */
            padding: 20px 40px;
            text-align: center;
            /* color: #777777; */
            font-size: 14px;
        }
        .footer a {
            color: #0047ab;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header"> <img
                src="<%= `${process.env.BASE_URL}/images/fischerbon-logo.png` %>"
                alt="FischerBon Logo" /> </div>
        <div class="content">
            <h1>
                <%= subject %>
            </h1>
            <p>
                <%= name ? `Dear ${name},` : "Hi" %>
                <%= "Dear user," %>
            </p>
            <p>
                <%= message %>
            </p>
            <p>Best regards,<br />The FischerBon Team</p>
        </div>
        <div class="footer">
            <p>&copy;
                <%= new Date().getFullYear() %>FischerBon. All
                rights reserved.
            </p>
            <p><a
                    href="<%= process.env.BASE_URL %>">www.fischerbon.com</a>
                | <a
                    href="mailto:support@fischerbon.com">support@fischerbon.com</a>
            </p>
        </div>
    </div>
</body>
</html>