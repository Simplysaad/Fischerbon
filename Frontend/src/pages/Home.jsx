import React from 'react';

function Home(props) {
    return (
        <div>
            Landing page(Login)
            Since students will register with the engr manually
            <br />
            Admin will approve the students
        </div>
    );
}

export default Home;